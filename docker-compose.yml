services:
  banco-agil:
    build: .
    container_name: banco_agil_full
    env_file:
      - .env
    environment:
      - BACKEND_WS_URL=ws://localhost:8000/chat/ws
      - STREAMLIT_SERVER_PORT=8501
    volumes:
      - ./data:/app/data
    ports:
      - "8000:8000"
      - "8501:8501"
